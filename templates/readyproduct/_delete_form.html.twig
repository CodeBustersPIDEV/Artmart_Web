<form method="post" action="{{ path('app_readyproduct_delete', {'readyProductId': readyproduct.readyProductId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ readyproduct.readyProductId) }}">
    <button class="btn">Delete</button>
</form>

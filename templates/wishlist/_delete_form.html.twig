<form method="post" action="{{ path('app_wishlist_delete', {'wishlistId': wishlist.wishlistId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wishlist.wishlistId) }}">
    <button class="btn">Delete</button>
</form>

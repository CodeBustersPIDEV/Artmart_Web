<form method="post" action="{{ path('app_shippingoption_delete', {'shippingoptionId': shippingoption.shippingoptionId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shippingoption.shippingoptionId) }}">
    <button class="btn">Delete</button>
</form>

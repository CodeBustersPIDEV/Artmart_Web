<form method="post" action="{{ path('app_categories_delete', {'categoriesId': category.categoriesId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.categoriesId) }}">
    <button class="btn">Delete</button>
</form>

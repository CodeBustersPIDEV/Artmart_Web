<form method="post" action="{{ path('app_productreview_delete', {'reviewId': productreview.reviewId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ productreview.reviewId) }}">
    <button class="btn">Delete</button>
</form>

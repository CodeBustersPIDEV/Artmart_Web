<form method="post" action="{{ path('app_orderrefund_delete', {'orderrefundId': orderrefund.orderrefundId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ orderrefund.orderrefundId) }}">
    <button class="btn">Delete</button>
</form>

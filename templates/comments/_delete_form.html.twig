<form method="post" action="{{ path('app_comments_delete', {'commentsId': comment.commentsId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.commentsId) }}">
    <button class="btn">Delete</button>
</form>

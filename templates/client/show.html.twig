{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ClientId</th>
                <td>{{ client.clientId }}</td>
            </tr>
            <tr>
                <th>NbrOrders</th>
                <td>{{ client.nbrOrders }}</td>
            </tr>
            <tr>
                <th>NbrDemands</th>
                <td>{{ client.nbrDemands }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_client_index') }}">back to list</a>

    <a href="{{ path('app_client_edit', {'clientId': client.clientId}) }}">edit</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('app_eventreport_delete_artist', {'reportid': eventreport.reportid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eventreport.reportid) }}">
    <button type="submit" class="btn btn-outline-danger">Delete</button>
</form>

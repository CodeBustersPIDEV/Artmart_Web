{% extends 'base.html.twig' %} 
{% import "base.html.twig" as base %}
{% block title %}Eventreport{% endblock %}

{% block body %}
{{ base.NavBar() }}
{% include('event/artist/eventBar.html.twig') %}

    <h1>Eventreport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Reportid</th>
                <td>{{ eventreport.reportid }}</td>
            </tr>
            <tr>
                <th>Attendance</th>
                <td>{{ eventreport.attendance }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('app_event_export_pdf', { id: eventreport.reportid }) }}" class="btn btn-primary">Details</a>

    <a href="{{ path('app_eventreport_index_artist') }}">back to list</a>

    <a href="{{ path('app_eventreport_edit_artist', {'reportid': eventreport.reportid}) }}">edit</a>

    {{ include('eventreport/artist/_delete_form.html.twig') }}
{% endblock %}

{% extends 'baseE.html.twig' %} 
{% block content %}
{% block body %}
	{% include('event/admin/eventBar.html.twig') %}

    <h1>Eventreport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Reportid</th>
                <td>{{ eventreport.reportid }}</td>
            </tr>
            <tr>
                <th>Attendance</th>
                <td>{{ eventreport.attendance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_eventreport_index') }}">back to list</a>

    <a href="{{ path('app_eventreport_edit', {'reportid': eventreport.reportid}) }}">edit</a>

    {{ include('eventreport/_delete_form.html.twig') }}
{% endblock %}
{% endblock %}

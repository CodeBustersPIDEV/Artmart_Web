{% extends 'baseE.html.twig' %} 
{% block content %}
{% block body %}
	{% include('event/admin/eventBar.html.twig') %}

    <h1>Eventreport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Reportid</th>
                <td>{{ eventreport.reportid }}</td>
            </tr>
            <tr>
                <th>Attendance</th>
                <td>{{ eventreport.attendance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_eventreport_index_admin') }}">back to list</a>

    <a href="{{ path('app_eventreport_edit_admin', {'reportid': eventreport.reportid}) }}">edit</a>

    {{ include('eventreport/admin/_delete_form.html.twig') }}
{% endblock %}
{% endblock %}

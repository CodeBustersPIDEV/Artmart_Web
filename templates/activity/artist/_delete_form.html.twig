<form method="post" action="{{ path('app_activity_delete_artist', {'activityid': activity.activityid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activity.activityid) }}">
    <button type="submit" class="btn btn-outline-danger">Delete</button>
</form>

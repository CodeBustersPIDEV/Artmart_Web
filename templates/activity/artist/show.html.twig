{% extends 'base.html.twig' %} 
{% import "base.html.twig" as base %}
{% block title %}Activity{% endblock %}

{% block body %}
{{ base.NavBar() }}
{% include('event/artist/eventBar.html.twig') %}

    <h1>Activity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Activityid</th>
                <td>{{ activity.activityid }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ activity.date ? activity.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ activity.title }}</td>
            </tr>
            <tr>
                <th>Host</th>
                <td>{{ activity.host }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_activity_index_artist') }}">back to list</a>

    <a href="{{ path('app_activity_edit_artist', {'activityid': activity.activityid}) }}">edit</a>

    {{ include('activity/artist/_delete_form.html.twig') }}
{% endblock %}

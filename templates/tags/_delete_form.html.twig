<form method="post" action="{{ path('app_tags_delete', {'tagsId': tag.tagsId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tag.tagsId) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Tags{% endblock %}

{% block body %}
    <h1>Tags</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>TagsId</th>
                <td>{{ tag.tagsId }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tag.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tags_index') }}">back to list</a>

    <a href="{{ path('app_tags_edit', {'tagsId': tag.tagsId}) }}">edit</a>

    {{ include('tags/_delete_form.html.twig') }}
{% endblock %}

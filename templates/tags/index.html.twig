{% extends 'base.html.twig' %}

{% block title %}Tags index{% endblock %}

{% block body %}
    <h1>Tags index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>TagsId</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tag in tags %}
            <tr>
                <td>{{ tag.tagsId }}</td>
                <td>{{ tag.name }}</td>
                <td>
                    <a href="{{ path('app_tags_show', {'tagsId': tag.tagsId}) }}">show</a>
                    <a href="{{ path('app_tags_edit', {'tagsId': tag.tagsId}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_tags_new') }}">Create new</a>
{% endblock %}

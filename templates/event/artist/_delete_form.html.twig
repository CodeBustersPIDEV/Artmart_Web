<form method="post" action="{{ path('app_event_delete_artist', {'eventid': event.eventid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.eventid) }}">
    <button type="submit" class="btn btn-outline-danger">Delete</button>
</form>

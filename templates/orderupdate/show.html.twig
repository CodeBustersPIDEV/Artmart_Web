{% extends 'baseO.html.twig' %} {% block content %} {% block stylesheets %}
<link rel="stylesheet" href="{{ asset('assets/Blog/CSS/AdminPage.css') }}" />
<style>
	.nav_links {
		cursor: pointer;
	}
	.adminSection {
		margin-left: 5px;
	}
</style>
{% endblock %} {% block body %}
    <h1>Orderupdate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>OrderupdateId</th>
                <td>{{ orderupdate.orderupdateId }}</td>
            </tr>
            <tr>
                <th>Updatemessage</th>
                <td>{{ orderupdate.updatemessage }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ orderupdate.date ? orderupdate.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_orderupdate_index') }}">back to list</a>

    <a href="{{ path('app_orderupdate_edit', {'orderupdateId': orderupdate.orderupdateId}) }}">edit</a>

    {{ include('orderupdate/_delete_form.html.twig') }}
{% endblock %}

{% endblock %} 
<form method="post" action="{{ path('app_orderstatus_delete', {'orderstatusId': orderstatus.orderstatusId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ orderstatus.orderstatusId) }}">
    <button class="btn">Delete</button>
</form>
